#include<stdio.h>
int main()
{
    int a[100],i,n,j,sum,k;
    scanf("%d",&n);
    for(i=0;i<n;i++)
    {
        scanf("%d",&a[i]);//1 2 3 4 5
    }
    scanf("%d",&k);
    for(i=0;i<n-1;i++)
    {
        for(j=i+1;j<n;j++)
        {
            sum=a[i]+a[j];
        
        if(sum==k)
        {
            printf("%d %d",a[i],a[j]);
        }
        }
    }
    return 0;
}
